@model IEnumerable<Billycock.Models.PlataformaCuenta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.usuariosdisponibles)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fechaPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.clave)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plataforma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cuenta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.usuariosdisponibles)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fechaPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plataforma.idPlataforma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cuenta.idCuenta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idPlataformaCuenta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idPlataformaCuenta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idPlataformaCuenta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
